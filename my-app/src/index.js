import React from "react"; // React17之后，不需要再进行引入
import ReactDOM from "react-dom";
import App from "./01-base/01-class_component";

ReactDOM.render(<App />, document.getElementById("root"));

/**
 * jsx == js + xml
 */
